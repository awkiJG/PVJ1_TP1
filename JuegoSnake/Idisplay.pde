// Interfaz Idisplay
// Define un contrato para que cualquier clase que la implemente
// esté obligada a definir el método display(), encargado de dibujar en pantalla.
interface Idisplay {
  
  // Método que deben implementar las clases que se pueden mostrar en pantalla.
  public void display();
}
